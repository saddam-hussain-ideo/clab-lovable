
// Define point values for quiz rewards
export const POINTS_PER_CORRECT_ANSWER = 100;
export const PERFECT_SCORE_BONUS = 500;

// Define maximum attempts for free users
export const MAX_FREE_QUIZ_ATTEMPTS = 3;

// Define quiz question timing
export const QUESTION_TIME_LIMIT = 20; // seconds

// Define storage keys
export const QUIZ_STORAGE_PREFIX = 'clab_quiz_';
